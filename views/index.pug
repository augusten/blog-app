doctype html
html
	head
		title osom blog
		link(rel='stylesheet' type='text/css' href='/css/materialize.min.css')
		link(rel='stylesheet' type='text/css' href='/css/main.css')
		link(href='http://fonts.googleapis.com/css?family=Roboto' rel='stylesheet' type='text/css')
		link(rel="stylesheet" type="text/css" href="//fonts.googleapis.com/css?family=Raleway")
	body
		if user
			ul.nav
				li
					a(href='/create') CREATE A POST
				if filter === "off"
					li
						a#filter(href='/filter') SHOW MY POSTS
				else
					li
						a#filter(href='/') SHOW ALL POSTS
				li
					a(href='/logout') LOG OUT
			div#header.valign-wrapper
				div.row.valign
					div.col.l12
						h1.center.white-text  Welcome, #{user.name}!
		else
			ul.nav
				li
					a(href='/register') REGISTER
				li
					a(href='/') POSTS
			div#header.valign-wrapper
				div.row
					div.col.l7
						h1.valign.center.white-text  Log in, anonymous
					div.col.l4
						//- LOG in form
						form( action='login' method='post' )
							label Email
								input( name='email' required )
							label Password
								input( name='password' type="password" required )
							.col.l6
								input.btn-flat.center-align.white-text#login(type='submit' value='Log me in')
							.col.l6
								div.white-text.bottom Don't have an accout yet? It's super easy to 
									a(href='/register') register
		//- Show all posts

		each item in posts
			.row
				.col.s6.offset-s3
					div#blogpost
						h5
							a(href="/showpost?title=" + item.title) #{item.title}
						div !{item.content}
	script(src='/js/jquery-3.1.1.min.js')
	script(src='/js/materialize.min.js')
	script(src='/js/main.js')
